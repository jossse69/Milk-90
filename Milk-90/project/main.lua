
function load()
    -- Initialization code here, if needed
end

function update(deltaTime)

end

function draw()
    clearScreen(0) -- Clear the screen
    drawText(0, 0, "Hello, World!") -- Draw text at position (10, 20)
end
